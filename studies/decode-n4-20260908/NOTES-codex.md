# N4 follow-up notes

Parent decode study and shared-memory dead ends already reviewed. No repetition of old Q8 half2/DMMV or rejected finer global32/lane-splitting mappings without new evidence. Source-only clone in a new directory; parent manifest remains sealed.

Hypothesis: parent half N4 underfills the machine. M4352,R2,B4 grid has68 CTAs; N8 with the SAME B4 kernel has136 CTAs. This is a new scheduling lead, not another accumulator substitution. Test few K partitions while preserving old half output bits. Counterpart mode9 keeps original sequential FP32 dots and must match archived FP32 bytes at S32.

CPU exact expression-tree checks prove residue-class partitioning and final P-way tree reproduce the original full S-way sum, including addition association. Each GPU partitioned half output is compared word-for-word to a dynamically rerun unpartitioned parent anchor on every fixture; independent CPU complete-dot checks retained. Geometry/occupancy API records are theoretical capacity, not profiler measurements.

Controller waits behind shared GPU3 holder PID1319131. Handoff request appended; no compilation or GPU work until acquired. All GPU work remains on GPU1 by UUID; no production or git publication action.

User steering: explicitly use unreserved GPUs. Cancelled global waiter before acquisition. GPU1 is free; switched to a GPU1-specific lock and coordination reservation while GPU3 may run concurrently. Original global serialization requirement is superseded by this user instruction for independent unreserved devices. Record concurrency and do not claim fully isolated host timing.

Completed four revisions and frozen confirmation. Coarse partitions plus R4 reuse and fused activation conversion win; R3/R6 and packed activation layout do not improve half performance, though R6 improves exact FP32 down/square. Confirmed 1.477–1.525× vs original FP32, 1.309–1.337× vs improved exact FP32. Single-arm confirmations agree. Broad finite-range half overflow persists. GPU1 released after 42 successful workers and six sanitizer runs; parent 544-file manifest unchanged. No production integration or publication action taken. See RESULTS.md and audit.json.

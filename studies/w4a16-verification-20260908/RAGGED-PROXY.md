# CPU-generated historical4096-token ragged proxy

Implemented/executed2026-09-08, CPU only. No CUDA/compiler/shared build or
production edits. Historical Q8 host-sort code routing, NOT current-Q4 timing
acceptance. All emitted JSON explicitly sets acceptance=false,gpu_executed=false.

## Superseded older generator and generated profiles

`route_proxy_profiles.py` and ALL JSON/stdout profiles generated by that older
script are SUPERSEDED for per-owner interpretation and timing-case selection.
Do not use their `selected.shards`, `global_experts`, owner counts or inferred
owner latency budgets as authentic historical owner shards. The script remains
untouched for provenance; its historical-Q8 disclaimer does not fix its ID-space
error. No separately named old JSON artifact was found in the scoped verification,
parent, tile256 or delivery-leads inventory; this notice applies to saved copies
and previously delivered stdout alike, not to the replacement ragged-proxy.json.

The older script indexes original-ID `placement-hot16.json.primary_owner` with
already EPLB-permuted runtime IDs from the trace. That silently re-shards real
counts into incorrect owner sets. Runtime expert//64 is the corrected contiguous
primary-owner PROXY. It still does not reconstruct actual hot/cohort service
ownership. Global expert counts and sums over all four old shards are preserved
by this wrong permutation; they do not validate any individual old shard.

Its old `m64_tiles=sum(ceil(M/64))` and `m32_tiles=sum(ceil(M/32))` describe TWO
alternative uniform tilings, not simultaneous current-policy tile classes.
Use ONLY `ragged_proxy.py`, `ragged-proxy.json`, and the mix/selected cases below
for the corrected proxy. All remain historical Q8 routing, not current-Q4
acceptance. Parent sources and historical generated outputs were not altered.

## Provenance and validation

ragged_proxy.py reads routes-code-16x4096.awtr in the ElasticWave worktree,
verifies SHA2568f78f5338327becbac14ed9c82f93b1199eb4d7bf6455fef5e3b58cc744038c3,
and NOW independently validates every record and route:640 records in16 ordered
samples of layers0..39,4096tokens each, exactly8 distinct expert IDs in0..255.
Each layer has32768 routes; complete file has20,971,520 routed rows.

The capture script enabled EPLB and the trace hook wrote raw runtime ids_read
values. Therefore primary-owner PROXY is runtime expert//64, not the original-ID
placement-hot16 primary_owner array applied a second time. No hot replication,
lane/diagonal subdivision or measured service ownership is inferred. Profile
identifiers preserve sample/layer/owner and64 runtime-local expert counts,
including zero-count experts. Original-ID recovery would need the capture EPLB
permutation, not a changed map assumed equivalent.

Tiling is derived from current source hashfee528c1270b053d37d56a88018c185a71f9e418a1fa572a1b086a669c574a47,
descriptor builder lines7118–7132: while remaining>=48, consume min64 asM64;
then remaining>16 consume min32 asM32; residual1..16 asM16. This is NOT naive
floor(M/64) greedy tiling:48..63 belongs toM64;33..47 yieldsM32 plusM16.
Three unit tests pass, including exhaustive row conservation for M0..4096.
Actual M16 cohorts/queue flags are NOT captured and are not fabricated.

## Aggregate profile over2560 owner observations

| T64 tile class | Tiles | Useful rows | Padded rows | Useful-work share |
| --- | ---: | ---: | ---: | ---: |
| M64 |288900|18238893|18489600|86.9698%|
| M32 |73707|2035597|2358624|9.7065%|
| M16 |88121|697030|1409936|3.3237%|

For fixed N,K, useful rows weight useful FLOPs exactly; these are NOT measured
time shares. M16 is~19.55% of450728 tiles but only3.32% useful work, and about
49.44% of its padded row slots are used. Combined M32/M16 represents13.03% of
useful work. Expert-size work shares differ from tile classes:

- M<=64:10.1836%; M65..128:14.9137%.
- M129..512:42.0814%; M>512:32.8213%.

A worker capped atM512 omits nearly one-third of this proxy's useful work.
Do not truncate hot experts or duplicate small profiles to disguise that gap.

## Smallest useful authentic-proxy timing matrix

Select real individual owner observations only from samples0..7; reserve8..15
for confirmation. No fabricated mixing/rescaling of expert counts. Selection
uses central counts/tailshare, nearest p90 tailshare and largest hot expert.
All three selected observations happen to belong to logical owner2; this is
not an all-owner performance claim. Aggregate mix above includes all4 owners.

| Case | Sample/layer/owner | Routed rows | Max M | Active experts | M64/M32/M16 tiles | Tail useful-work share |
| --- | --- | ---: | ---: | ---: | --- | ---: |
| central |4/1/2|8158|904|62|113/30/26|12.7973%|
| p90 tail |3/1/2|8014|612|61|107/33/36|15.4729%|
| hot expert |5/39/2|10076|3684|57|147/18/37|7.3243%|

Absolute minimum to expose tail sensitivity: first TWO cases x gate/up
N512,K2048 and downN2048,K512 = FOUR paired timing shapes. Recommended bounded
matrix adds hot case x both projections = SIX shapes, because otherwise
M>512 work stays poorly represented. Same expert counts apply to gate/up/down;
gate and up are two separate projections, not one free fused output.

Use unmodified current-policy M64/M32/M16 baseline dispatch, not padded M64-only,
plus candidate and its fallback on IDENTICAL count lists. Check output poisoning,
full numerical identity, independent CPU samples and sanitizers before timing.
Activation values/weight fixtures remain synthetic unless explicitly loaded from
real artifacts; charge their real preparation and label the run accordingly.
All job-list creation/queue assembly that runs per pipeline must be charged or
excluded identically with an explicit setup boundary. No performance measured
by this generator, no current-Q4 or distributed-critical-path gain inferred.

Read selected counts_cli and jobs from ragged-proxy.json. Jobs specify runtime
expert ID, local slot, row offset, useful rows and tile class, with zeros retained
in counts but no zero-row launch. Replay implementation needs genuinely ragged
addressing, M up to3684, and current tails/cohorts; merely raising the equal-M
worker CLI bound cannot consume these cases correctly. Source token IDs can be
recovered by selected sample/layer from the trace if an indexed-gather proxy is
later desired; activation bytes/router weights are absent, so no authentic
activation gather is claimed by this count/job-only artifact.

Freeze candidate after6-shape screen, then independently select held-out
samples8..15 for all logical owners. Aggregate matched COMPLETE times as sums,
never average speedups; count gate/up twice and down once only for an explicitly
modeled3-projection timing summary. This remains historicalQ8 proxy evidence.

## Reproduction and changed files

```sh
PYTHONDONTWRITEBYTECODE=1 python3 studies/w4a16-verification-20260908/test_ragged_proxy.py
PYTHONDONTWRITEBYTECODE=1 python3 studies/w4a16-verification-20260908/ragged_proxy.py --output studies/w4a16-verification-20260908/ragged-proxy.json
```

New files: ragged_proxy.py, test_ragged_proxy.py, ragged-proxy.json (generated
selected counts/jobs plus aggregate profile), RAGGED-PROXY.md. Output option
rejects destinations outside owned verification directory. No parent source,
production files, coordination logs or W4A4 artifacts modified.

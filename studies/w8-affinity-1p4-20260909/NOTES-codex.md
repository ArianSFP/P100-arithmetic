2026-09-09: W8A16 dual FP16 accumulation optimized to 1.7293x GU / 1.7664x down at M256; existing M64 descriptor integration including pair+fallback launches confirms 1.7080x / 1.7482x (three fresh workers each, faster original input branch as control). Main changes: M128 reuse, paired-K shared B words, double-buffered B, two independent FP16 rails. See RESULTS.md for exact scope, raw evidence and closed leads. Synthetic accuracy differs from original; PPL/KLD gate and full-model prefill are unrun because the original 37.8GB Q8 GGUF was deleted and only ~11GB disk is free. No accuracy-qualified promotion. All selected GPU workers and locks released after runs.

2026-09-09, 1.9x follow-up: 36 new variants screened. Target not reached. Three-worker confirmed complete pipelines: F32 storage 1.786183x GU / 1.842659x down; FP16 storage 1.835868x / 1.885056x. Serving profile WIRE=f32 makes the first row directly relevant. Improvements: interleaved dual rails, exact paired-Q8 half decoder, storage-specific loaders, dimension specialization, compact GPU pairing/leftovers (all three launches charged). All retain prior dual-half arithmetic; model PPL remains unqualified. Isolated model mode 6 builds the F32 implementation with per-stream scratch and caller tile capacities. See TARGET-1P9.md, manifests and raw logs. Missing Q8 model / ~11 GB free still blocks full-model gates. No production changes or commits.
